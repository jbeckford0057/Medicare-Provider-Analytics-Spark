# Raw Data

Raw CMS source datasets are intentionally not stored in GitHub because of their size.

The cloud deployment stores the source data in Amazon S3 under the project bucket:

`s3://medicare-provider-analytics-cs675-2026/raw/`

Logical source prefixes include:

- `provider_services/`
- `provider_enrollment/`

This directory is retained only to document the expected local project structure.
